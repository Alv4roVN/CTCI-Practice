from typing import List


def zero_matrix(matrix: List[List[int]]) -> List[List[int]]:
    """
    CTCI 1.8 - Zero Matrix
    If an element in an MxN matrix is 0, set its entire row and column
    to 0, in place.

    Args:
        matrix: an MxN matrix, mutated in place.

    Returns:
        The same matrix object (returned for test convenience).
    """
    raise NotImplementedError
