from typing import List


def search_rotated(arr: List[int], target: int) -> int:
    """
    CTCI 10.3 - Search in Rotated Array
    Given a sorted array of distinct integers that has been rotated an
    unknown number of times, find the index of target.

    Args:
        arr: rotated, sorted array of distinct integers.
        target: value to find.

    Returns:
        The index of target in arr, or -1 if not present.

    Example:
        search_rotated([4, 5, 6, 7, 0, 1, 2], 0) -> 4
        search_rotated([4, 5, 6, 7, 0, 1, 2], 3) -> -1
    """
    raise NotImplementedError
