def string_compression(s: str) -> str:
    """
    CTCI 1.6 - String Compression
    Perform basic string compression using counts of repeated characters,
    e.g. "aabcccccaaa" -> "a2b1c5a3". If the compressed string would not
    become smaller than the original, return the original string unchanged.

    Args:
        s: input string.

    Returns:
        The compressed string, or the original if compression doesn't help.
    """
    raise NotImplementedError
